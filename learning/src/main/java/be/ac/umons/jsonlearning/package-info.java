/**
 * Oracles for learning an automaton from a JSON document.
 * 
 * <p>
 * Both membership and equivalence queries are defined for three types of
 * automata: visibly pushdown automata (VPDA), visibly one-counter automata
 * (VCA), and realtime one-counter automata (ROCA).
 * </p>
 * 
 * @author Gaëtan Staquet
 */
package be.ac.umons.jsonlearning;
